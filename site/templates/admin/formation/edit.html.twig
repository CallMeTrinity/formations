{% extends 'base.html.twig' %}

{% block title %}Admin — {{ formation.title }}{% endblock %}

{% block body %}
    <div class="container-page container-narrow py-12 space-y-8">
        <div>
            <p class="eyebrow mb-2">Espace admin</p>
            <h1 class="text-2xl font-semibold tracking-tight">{{ formation.title }}</h1>
            <p class="text-sm text-fg-secondary mt-1">
                Métadonnées admin. Elles sont préservées par la synchronisation du contenu.
            </p>
            <a href="{{ path('app_admin_formations') }}" class="text-sm text-fg-secondary no-underline font-medium hover:text-fg inline-block mt-3">
                ← Retour à la liste
            </a>
        </div>

        <div class="card max-w-md">
            {{ form_start(form, {attr: {class: 'space-y-6', novalidate: 'novalidate'}}) }}
                {% if not form.vars.valid %}
                    <div class="callout callout--danger" role="alert">
                        <div class="callout__title">Vérifie le formulaire</div>
                    </div>
                {% endif %}

                <div class="space-y-1.5">
                    {{ form_label(form.status) }}
                    {{ form_widget(form.status) }}
                    {% for error in form.status.vars.errors %}
                        <p class="text-xs text-danger">{{ error.message }}</p>
                    {% endfor %}
                </div>

                <div class="space-y-1.5">
                    {{ form_label(form.difficulty) }}
                    {{ form_widget(form.difficulty) }}
                    {% for error in form.difficulty.vars.errors %}
                        <p class="text-xs text-danger">{{ error.message }}</p>
                    {% endfor %}
                </div>

                <div class="space-y-1.5">
                    {{ form_label(form.estimatedMinutes) }}
                    {{ form_widget(form.estimatedMinutes) }}
                    {% for error in form.estimatedMinutes.vars.errors %}
                        <p class="text-xs text-danger">{{ error.message }}</p>
                    {% endfor %}
                </div>

                <button class="btn btn--primary focus-ring" type="submit">Enregistrer</button>
            {{ form_end(form) }}
        </div>

        {# Tags : sélection enregistrée immédiatement (turbo frame), indépendante
           des métadonnées ci-dessus. Le champ de recherche est hors de la frame
           pour survivre aux rechargements ; le filtrage est ré-appliqué à chaque
           chargement de la frame. #}
        <div class="card max-w-md space-y-4">
            <div>
                <h2 class="text-sm font-semibold text-fg">Tags</h2>
                <p class="field-help">Coche un tag pour l'ajouter : c'est enregistré aussitôt. Les tags sont aussi proposés dans les préférences.</p>
            </div>

            <div
                data-controller="tag-picker"
                data-action="turbo:frame-load->tag-picker#filter"
                class="space-y-3"
            >
                <input
                    type="search"
                    data-tag-picker-target="search"
                    data-action="input->tag-picker#filter"
                    placeholder="Rechercher un tag…"
                    aria-label="Rechercher un tag"
                    class="input focus-ring"
                    autocomplete="off"
                >

                {{ include('admin/formation/_tags_frame.html.twig', {
                    formation: formation,
                    all_tags: all_tags,
                    message: null,
                    error: null,
                }) }}
            </div>
        </div>
    </div>
{% endblock %}
